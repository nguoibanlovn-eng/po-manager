"use client";

// Removed — instant tab switching caused auth issues with API fetch.
// Using per-view DashXxxSwitch instead (stable server-rendered approach).
export default function DashMobileWrapper() { return null; }
